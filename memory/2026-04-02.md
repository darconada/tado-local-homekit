- 2026-04-02 08:48 UTC — Tado reverse-engineering breakthrough: for Dani's older Tado bridge (`IB01`, IP `192.168.200.10`), the strongest public path is not the cloud REST API but the bridge's built-in HomeKit Accessory Protocol on port 80. Community project `AmpScm/TadoLocal` uses `aiohomekit` with `HOMEKIT_DEFAULT_PORT = 80` and `post_tlv("/pair-setup")` to achieve local state/control. This matches observed behavior: port 80 accepts TCP but does not behave like a normal web UI.
- Key constraint: the old Tado Internet Bridge appears to allow only one HomeKit controller at a time, so existing Apple Home / HA HomeKit Controller pairings may need removal or a HomeKit reset on the bridge before local pairing.
- Practical takeaway: local temperature/humidity/heating-state telemetry per zone/planta looks feasible today via HomeKit, without depending on Tado cloud polling. Some metadata/features (e.g. zone names/battery/persistent schedule semantics) may still be hybrid or need extra work.
- 2026-04-02 10:33 UTC — Successfully paired locally to the old Tado Internet Bridge (`IB0090508800`, `192.168.200.10`) via HomeKit/HAP using setup code `653-11-104`. Verified that the bridge speaks HAP on port 80 and that HomeKit pairing works after clearing the unavailable state.
- Reading accessories via HomeKit returned 2 accessories: the Internet Bridge metadata and one thermostat accessory for `RU0572525568`. Current locally readable values from HomeKit: current temperature 16.7 °C, target temperature 16.0 °C, humidity 63%, current heating/cooling state 0 (off), target heating/cooling state 1 (heat), display units Celsius.
- Important operational note: HomeKit pairing on the Tado Internet Bridge was initially blocked with `UnavailableError`; after the bridge became pairable, pairing succeeded. The Tado app's “pairable for 12 hours” appears to refer to Tado internal pairing, not necessarily immediate HomeKit availability.
- Mapping confirmed by Dani: `RU0572525568` corresponds to `planta 2`. This is the first confirmed correlation between the HomeKit-exposed accessory tree and the real house zones/plants.
- Full Tado room mapping confirmed by Dani:
  - `RU2165705728` -> `PLANTA 1`
  - `RU0572525568` -> `PLANTA 2`
  - `RU3827043328` -> `ATICO`
  - `RU3948940800` -> `MERENDERO`
- `BU0542969600` is the Tado Extension Kit responsible for hot water control. It is not a room thermostat; it controls domestic hot water.
- Further TadoLocal research: the project is explicitly hybrid, not pure-HomeKit. `sync.py` populates zones/devices from Tado Cloud (`zones`, `deviceList`, `zoneStates`) and marks zone leaders based on cloud `duties` like `ZONE_LEADER`/`CIRCUIT_DRIVER`, while HomeKit is the real-time source for local events and control. This explains why a raw HomeKit accessory dump can expose fewer devices/zones than the final API/UI.
- 2026-04-02 17:39 UTC — Re-reading the HomeKit accessory tree after the afternoon pause showed the full local thermostat set, not just one zone. HAP now exposes AIDs 2-5 for all four RU thermostats: RU0572525568 (PLANTA 2), RU3827043328 (ATICO), RU2165705728 (PLANTA 1), RU3948940800 (MERENDERO), each with current temperature, target temperature, target/current HVAC state, and humidity. This strongly supports that Plan A (pure local via HomeKit) can cover all heating zones; hot water/Extension Kit still remains separate and may stay on cloud if needed.
- 2026-04-02 17:43 UTC — Successfully wrote local HomeKit control to PLANTA 1 (`RU2165705728`, AID 4): target temperature changed from 16.0 °C to 18.0 °C via HAP (`TargetHeatingCoolingState=1`, `TargetTemperature=18.0`) and readback confirmed the new setpoint. This validates local write control for heating zones, not just read-only telemetry.
- Dani confirmed from the official Tado app that PLANTA 1 showed the new 18 °C setpoint immediately after the local HomeKit write. This externally validates that the HAP write propagated correctly into the real Tado system, not just the local cache/readback.
- 2026-04-02 17:55 UTC — Deployed a production-style local backend on `domo-server` (`192.168.200.2`) under `/opt/tado-local-homekit` as systemd service `tado-local-homekit.service`, listening on `:4407`. It uses the paired HomeKit credentials and exposes HTTP endpoints `/status`, `/zones`, `/zones/{slug}`, and `/zones/{slug}/set`. Verified remotely and from the LAN: the API reports all four zones (`planta1`, `planta2`, `atico`, `merendero`) with local temperatures/humidity/setpoints.
- 2026-04-02 19:05 UTC — Dani confirmed in Home Assistant that the new local entities from the custom component are visible and that testing PLANTA 2 through the HomeKit-backed integration worked correctly. This validates the HA integration path end-to-end for at least one zone in the real UI.
- 2026-04-02 22:41 UTC — Root cause found for local Tado write hang: backend deadlocked on writes because `set_zone()` acquired the global asyncio lock and then called `get_zone(force=True)`, which tried to acquire the same non-reentrant lock again. Fixed by reading back the written zone directly under the existing lock instead of re-entering `get_zone()`. After redeploy, backend writes for PLANTA 2 (`off` and `heat`) returned 200 and readback confirmed the state transition without hanging.
